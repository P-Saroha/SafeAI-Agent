[client]
# Disable the local file watcher that tries to inspect all modules
showErrorDetails = false

[logger]
# Suppress verbose logging
level = "error"

[client]
# Disable Streamlit's aggressive module inspection
toolbarMode = "minimal"
